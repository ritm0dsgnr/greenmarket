# Development and test may run without a database until the catalogue stage.
# Production startup requires both variables and validates their format.
DATABASE_URL=postgresql://application_user:change-me@localhost:5432/greenmarket
APP_BASE_URL=https://example.invalid
